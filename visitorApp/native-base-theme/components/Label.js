// @flow

import variable from "./../variables/platform";

export default (variables /*: * */ = variable) => {
  const labelTheme = {
    ".focused": {
      width: 0
    },
    marginLeft : 10,
    fontSize: 17,
    justifyContent : 'center'
  };

  return labelTheme;
};
